# Public API base URL — must include /api, no trailing slash. Restart Vite after changes.
# Example: http://localhost:5000/api
VITE_API_BASE_URL=http://localhost:5000/api

# Optional: canonical site URL for documentation / future SSR (not required for Vite client build)
# VITE_PUBLIC_SITE_URL=http://localhost:5173
